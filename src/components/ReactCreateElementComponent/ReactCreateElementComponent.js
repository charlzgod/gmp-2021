import React, { createElement } from 'react'

const ReactCreateElementComponent = function () {
  return createElement('h1', {}, 'Hello React Create Element Component!')
}

export default ReactCreateElementComponent
